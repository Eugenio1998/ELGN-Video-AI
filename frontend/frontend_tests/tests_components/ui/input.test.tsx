import { render, screen } from "@testing-library/react";

describe("input", () => {
  it("deve renderizar corretamente", () => {
    // TODO: adicionar renderização real do componente
    expect(true).toBe(true);
  });
});
